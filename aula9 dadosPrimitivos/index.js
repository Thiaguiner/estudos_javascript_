//Tipos de dados primitivos, String, number, null, undefined, boolean.
const nome = 'Thiago' //string
const nome1 = "Thiago" //string
const nome2 = `Thiago`//string
const num1 = 10; //number
const num2 = 10.52 //number
let nomeAluno; //undefined = não aponta para local nenhum na memoria(valor nulo)
let sobrenomeAluno = null //nulo -> Não aponta para local nenhum na memoria /// utiliza null quando quer, para somente salvar
const aprovado = false; // boolean = true, false (lógico)

console.log(typeof num1, num1)